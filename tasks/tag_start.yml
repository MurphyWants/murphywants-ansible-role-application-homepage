---
  - name: Check if docker-compose file exists
    ansible.builtin.stat:
      path: "{{ HOMEPAGE_PATH }}/docs/docker-compose.yml"
    register: homepage_stat_docker_compose_file

  - name: Docker Compose Start Apps
    community.docker.docker_compose_v2:
      project_src: "{{ HOMEPAGE_PATH }}/docs"
      files: "docker-compose.yml"
      state: present
      pull: always
      project_name: homepage
    when: homepage_stat_docker_compose_file.stat.exists

